# Copy to .env in the repo root. Used by db:push, db:seed, and dev:api.
# Database (SQLite for local development)
DATABASE_URL="file:./dev.db"

# Session cookie (required for API)
SESSION_SECRET="your-secret-at-least-32-chars-use-openssl-rand-hex-32"

# Optional: CORS origin for API (default: http://localhost:5173)
ALLOWED_ORIGIN="http://localhost:5173"

# Optional: API base URL for Vue app (default: proxy to localhost:3000 in dev)
# VITE_API_URL="http://localhost:3000"
